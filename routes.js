/** @format */

const API = "/api";
const GREETING = "/greeting";
const GREETINGS = "/greetings";
const routes = {
  api: API,
  greeting: GREETING,
  greetings: GREETINGS,
};

export default routes;
