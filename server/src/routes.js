/** @format */

const HOME = "/";

const API = "/api";
const POSTS = "/posts";
const UPLOAD = "/upload";

const routes = {
  home: HOME,
  api: API,
  upload: UPLOAD,
  posts: POSTS,
};

export default routes;
