/** @format */

const kakaoLogin = "/auth/kakao";

const posts = "/api/posts";
const upload = "/api/upload";

const routes = {
  kakao: kakaoLogin,
  upload: upload,
  posts: posts,
};

export default routes;
